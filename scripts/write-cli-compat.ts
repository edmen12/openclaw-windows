#!/usr/bin/env node
// Windows-only: daemon-cli compatibility not needed for Windows builds
// The legacy CLI compatibility layer is primarily for Linux/macOS service management
console.log("Windows-only build: Skipping daemon-cli legacy CLI compatibility check");
